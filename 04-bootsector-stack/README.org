[[file:./README_en.md][English]]

/事先需要 Google 的概念：stack(栈)/

*目标: 学习如何使用栈*

栈的使用是很重要的，因此我们另写了一个启动扇区程序作为例子。

记住， =bp= 寄存器存放的是栈的基地址（栈底）， =sp= 则存放的是栈顶，栈的增长是面向 =bp= 方向（ =sp= 递减）

这次课程相当直接，直接跳转到代码头部。

我建议你尝试自己使用栈内的内存地址，在和本代码不同的部分，看会发生什么。
